services:
  - type: web
    name: expense-model-api
    env: python
    plan: free
    rootDir: finance-api
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    autoDeploy: true
